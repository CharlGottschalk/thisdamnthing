# Shipped source

The `dryft` Python package and its installed resources live here. See
[the source layout](../docs/mvp.md#source-layout) for the directory structure. No installed workspace state belongs in this directory.
